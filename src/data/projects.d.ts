import type { Project } from "../types";
/**
 * The single source of truth for everything that appears on syso.codes.
 * Keep this file alphabetized within each section to make diffs readable.
 *
 * To regenerate / update from GitHub, see CLAUDE.md → "Update workflow".
 */
export declare const projects: Project[];
